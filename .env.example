# Copy this file to ".env" and paste your Supabase values.
# Find them in Supabase → Project Settings → API.
# If you leave these blank, the app runs in offline-demo mode (single device).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
